name: revive_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  http: ^1.1.0
  flutter_dotenv: ^5.1.0
  provider: ^6.0.5
  pusher_client: ^2.0.0
  qr_flutter: ^4.0.0
  mobile_scanner: ^2.1.0
  shared_preferences: ^2.0.15  # Moved here properly

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
